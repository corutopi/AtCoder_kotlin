package ABC.at0XX.at001

fun solveX(H1: Int, H2: Int) {
    println(H1 - H2)
}

fun main() {
//    val s = readLine()!!
    val H1 = readLine()!!.toInt()
    val H2 = readLine()!!.toInt()
//    val (n, m) = readLine()!!.split(" ").map { it.toInt() }
//    val a = readLine()!!.split(" ").map { it.toInt() }
    solveX(H1, H2)
}