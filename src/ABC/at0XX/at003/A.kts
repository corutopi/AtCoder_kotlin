
fun solveX(n: Int) {
    println((10000 * ((n + 1) / 2.0)).toInt())
}

fun main() {
//    val s = readLine()!!
    val n = readLine()!!.toInt()
//    val (n, m) = readLine()!!.split(" ").map { it.toInt() }
//    val a = readLine()!!.split(" ").map { it.toInt() }
    solveX(n)
}